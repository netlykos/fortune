plugins {
  // Apply the common convention plugin for shared build configuration between library and application projects.
  id "org.netlykos.fortune.java-common"

  // Apply the quarkus plugin to add support for building a quarkus application
  id "io.quarkus"
}

